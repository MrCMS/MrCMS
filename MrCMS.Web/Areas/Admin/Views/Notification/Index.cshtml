@using MrCMS.Entities.Notifications
@using MrCMS.Helpers
@using MrCMS.Paging
@using MrCMS.Web.Areas.Admin.ACL
@model MrCMS.Web.Areas.Admin.Models.NotificationSearchQuery
@{
    var results = ViewData["results"] as IPagedList<Notification> ?? PagedList<Notification>.Empty;
    var notificationTypeOptions = ViewData["notification-type-options"] as List<SelectListItem> ?? new List<SelectListItem>();
}
@using (Html.BeginForm("Index", "Notification", FormMethod.Get))
{
    <fieldset>
        <legend>Notifications
            <span class="btn-group display-inline pull-right">
                <input type="submit" class="btn btn-primary" value="Search" />
                @Html.ActionLink("Push", "Push", "Notification", new { }, new { @class = "btn", data_toggle = "fb-modal" })
            </span>
        </legend>

        <div class="row-fluid">
            <div class="span4">
                @Html.LabelFor(query => query.Message)
                @Html.TextBoxFor(query => query.Message, new { @class = "span12" })
            </div>
            <div class="span4">
                @Html.LabelFor(query => query.NotificationType)
                @Html.DropDownListFor(query => query.NotificationType, notificationTypeOptions, new { @class = "span12" })
            </div>
            <div class="span2">
                @Html.LabelFor(query => query.From)
                @Html.TextBoxFor(query => query.From, new { @class = "date-time-picker span12" })
            </div>
            <div class="span2">
                @Html.LabelFor(query => query.To)
                @Html.TextBoxFor(query => query.To, new { @class = "date-time-picker span12" })
            </div>
        </div>
    </fieldset>
}

@if (results.Count == 0)
{
    @:No notifications.
        }
else
{
    <table class="table table-striped table-condensed" style="overflow: scroll">
        <thead>
            <tr>
                <th>Date
                </th>
                <th>Notification Type
                </th>
                <th>Message
                </th>
                <th>User
                </th>
                <th>Actions
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var notification in results)
            {
                <tr>
                    <td>
                        @notification.CreatedOn.ToString("dd/MM/yyyy HH:mm")
                    </td>
                    <td>
                        @notification.NotificationType.ToString().BreakUpString()
                    </td>
                    <td>
                        @Html.Raw(notification.Message)
                    </td>
                    <td>
                        @notification.UserName
                    </td>
                    <td>
                        <div class="btn-group">
                            @if (Html.CanAccess<NotificationACL>(NotificationACL.Delete))
                            {
                                @Html.ActionLink("Delete", "Delete", "Notification", new { id = notification.Id }, new { @class = "btn btn-mini btn-danger", data_toggle = "fb-modal" })
                            }
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}
@Html.Pager(results, "Index", page => new { page })