@model dynamic
@using (Html.BeginForm("Migrate", "FileMigration", FormMethod.Post))
{
    <input type="submit" value="@Resource("Admin Queue File Migration", "Queue any required migrations")" class="btn btn-xs btn-primary" />
}