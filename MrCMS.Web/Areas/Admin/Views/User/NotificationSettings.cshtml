@using MrCMS.Entities.UserProfile
@model MrCMS.Entities.People.User
@{
    var settings = Model.Get<NotificationSettings>();
}
<fieldset>
    <legend>
        Notification Settings
    </legend>
    @{ Html.RenderAction("Show", "NotificationSettings", new { settings, user = Model }); }
</fieldset>
