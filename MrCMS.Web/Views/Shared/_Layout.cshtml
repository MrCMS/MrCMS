@using MrCMS.Helpers
@using MrCMS.Website
@model MrCMS.Entities.Documents.Web.Webpage
<!DOCTYPE html>
<html lang="en">
<head>
    <title>@(!string.IsNullOrWhiteSpace(Model.MetaTitle) ? Model.MetaTitle : Model.Name)</title>
    <meta name="description" content="@Model.MetaDescription" />
    <meta name="keywords" content="@Model.MetaKeywords" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="@Url.Content("~/Content/bootstrap/css/bootstrap.min.css")" rel="stylesheet" />
    <link href="@Url.Content("~/Content/bootstrap/css/bootstrap-responsive.min.css")" rel="stylesheet" />
    <link href="@Url.Content("~/Content/Styles/style.css")" rel="stylesheet" />
    <!-- Le HTML5 shim, for IE6-8 support of HTML5 elements -->
    <!--[if lt IE 9]><script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script><script src="/Content/Scripts/respond.min.js"></script><![endif]-->
    @RenderZone("Head Files")

    @{ Html.RenderAction("RenderAnalytics", "SEO"); }
</head>
<body>

    <div class="logo-container">
        <div class="container">
            <a href="/" style="margin: 0; padding: 0">
                <img src="@Url.Content("~/Content/images/mrcms-logo.png")"></a>
        </div>
    </div>

    <div class="navbar navbar-inverse navbar-fixed-top nav-container">
        <div class="navbar-inner">
            <div class="container">
                <a href="/" data-toggle="collapse" data-target=".nav-collapse" class="logo-small"></a>
                <button type="button" class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <div class="nav-collapse collapse">
                    @RenderZone("Main Navigation")
                </div>
            </div>
        </div>
    </div>

    <div class="container">
        @RenderZone("Before Content")
        @RenderBody()
        @RenderZone("After Content")
    </div>

    <footer>
        <div class="container">
            Mr CMS @DateTime.Now.Year
        </div>
    </footer>
    <script type="text/javascript" src="//ajax.googleapis.com/ajax/libs/jquery/1.8.3/jquery.min.js"></script>
    <script src="@Url.Content("~/Content/bootstrap/js/bootstrap.min.js")" type="text/javascript"></script>
    <script src="@Url.Content("~/Content/Scripts/jquery.validate.min.js")" type="text/javascript"></script>
    <script src="@Url.Content("~/Content/Scripts/jquery.validate.unobtrusive.min.js")" type="text/javascript"></script>

    @if (MrCMSApplication.CurrentUserIsAdmin)
    {
        Html.RenderAction("InPageEditor", "InPageAdmin", new { id = Model.Id, area = "Admin" });
    }

    @if (IsSectionDefined("JavascriptBlockInclude"))
    {
        @RenderSection("JavascriptBlockInclude")
    }

</body>
</html>
