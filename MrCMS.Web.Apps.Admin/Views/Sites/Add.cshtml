@using MrCMS.Helpers
@using MrCMS.Models
@model MrCMS.Web.Apps.Admin.Models.AddSiteModel

@using (Html.BeginForm("Add", "Sites", FormMethod.Post))
{
    <fieldset>
        <legend>
            @Html.Resource("Admin Add Site", "Add site")
            <span class="btn-group  display-inline  pull-right">
                <input type="submit" class="btn-primary btn" value="@Html.Resource("Save")" />
            </span>
        </legend>
        <div class="row">
            <div class="col-lg-12">
                <div class="form-group">
                    @Html.LabelFor(x => x.Name)
                    @Html.TextBoxFor(x => x.Name, new { @class = "form-control" })
                    @Html.ValidationMessageFor(x => x.Name)
                </div>
                <div class="form-group">
                    @Html.LabelFor(x => x.BaseUrl)
                    @Html.TextBoxFor(x => x.BaseUrl, new { @class = "form-control" })
                    @Html.ValidationMessageFor(x => x.BaseUrl)
                </div>
            </div>
        </div>
        <div class="row">
            <div class="col-lg-12">
                <partial name="CloneOptions" model="Model"/>
            </div>
        </div>
    </fieldset>
}
@{ Html.IncludeScript("~/Areas/Admin/Content/Scripts/mrcms/add-site.js");}