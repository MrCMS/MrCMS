@using MrCMS.Helpers
@model MrCMS.Entities.Documents.Web.UrlHistory
@{ Layout = "~/Areas/Admin/Views/Shared/_LayoutPopup.cshtml";}
@using (Html.BeginForm("Delete", "UrlHistory", FormMethod.Post))
{
    <fieldset>
        <legend>@Resource("Admin Webpage Url History Delete", "Delete Url")
            <span class="btn-group  display-inline  pull-right">
                <input type="submit" class="btn-danger btn" value="Delete" />
            </span>
        </legend>
        @Html.InfoBlock(Resource("Heads up") + "!", Resource("Admin Delete Info Warning", "Deleting this cannot be undone."), AlertType.danger)


    </fieldset>
}