@model System.Collections.Generic.IList<MrCMS.Entities.Multisite.Site>

<ul class="dropdown-menu site-list" role="menu">
    @foreach (var site in Model)
    {
        <li>
            @Html.Link(site.Name, site.BaseUrl) @*<a href="@site.GetFullDomain">@site.Name</a>*@
        </li>
    }
</ul>