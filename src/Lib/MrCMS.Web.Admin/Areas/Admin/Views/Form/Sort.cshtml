@using System.Linq
@using Microsoft.AspNetCore.Mvc.TagHelpers
@model System.Collections.Generic.List<MrCMS.Models.SortItem>
@{ Layout = "~/Areas/Admin/Views/Shared/_LayoutPopup.cshtml"; }

<form asp-controller="Form" asp-action="Sort" class="form-update-form" data-form-sort-form id="sort-fields-form">
    <fieldset>
        <legend>
            @await Html.Resource("Admin Form Sort Fields", "Sort fields")
            <span class="btn-group pull-right">
                <button class="btn btn-primary" id="submit">@await Html.Resource("Admin Form Sort Save", "Save & Close")</button>
            </span>
        </legend>

        <ul id="sortable" class="list-group">
            @if (Model.Any())
            {
                for (var i = 0; i < Model.Count; i++)
                {
                    <li class="list-group-item">
                        @Html.HiddenFor(model => model[i].Id)
                        @Html.HiddenFor(model => model[i].Order)
                        <i class="fa fa-arrows-alt"></i>&nbsp;&nbsp;
                        @Model[i].Name
                    </li>
                }
            }
            else
            {
                <li>@await Html.Resource("Admin Form No form fields", "No form field").</li>
            }
        </ul>
    </fieldset>
</form>