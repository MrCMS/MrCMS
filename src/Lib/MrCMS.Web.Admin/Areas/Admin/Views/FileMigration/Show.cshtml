@model dynamic
@using (Html.BeginForm("Migrate", "FileMigration", FormMethod.Post))
{
    <input type="submit" value="@await Html.Resource("Admin Queue File Migration", "Queue any required migrations", options => options.DisableInlineEditing())" class="btn btn-sm btn-primary"/>
}