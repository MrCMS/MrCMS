@model MrCMS.Entities.Documents.Web.UrlHistory
@{
    Layout = "~/Areas/Admin/Views/Shared/_LayoutPopup.cshtml";
}

<form asp-controller="Redirects" asp-action="MarkAsGone" asp-route-id="@Model.Id">
    @Html.ReturnToReferrer()
    <fieldset>
        <legend>
            Mark as gone
            <span class="btn-group display-inline pull-right">
                <button type="submit" class="btn btn-danger">Mark as gone</button>
            </span>
        </legend>
        <p>
            @string.Format(await Html.Resource("Redirects - Mark as gone", "Are you sure you wish to mark '{0}' as gone?"), Model.UrlSegment)
        </p>
    </fieldset>
</form>