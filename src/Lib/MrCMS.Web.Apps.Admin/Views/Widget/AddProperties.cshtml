@using MrCMS.Helpers
@using MrCMS.Web.Apps.Admin.Infrastructure.Helpers
@model object
@{
    Type type = ViewData["type"] as Type;
}

@if (Model != null)
{
    @await Html.RenderCustomAddAdminProperties(type, Model)
}