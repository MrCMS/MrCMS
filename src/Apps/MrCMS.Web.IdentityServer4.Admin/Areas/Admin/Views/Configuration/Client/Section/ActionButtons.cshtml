@using Microsoft.AspNetCore.Mvc.Localization
@using MrCMS.Services.Resources
@model MrCMS.Web.Apps.IdentityServer4.Admin.Core.Dtos.Configuration.ClientDto
@inject IStringResourceProvider  Localizer

<div class="row client">
    <div class="col-md-12 action-butons">
        <button class="client-save-button btn btn-primary">@Localizer.GetValue("ButtonSave", "Save Client")</button>

        @if (Model.Id != 0)
        {
            @*<a asp-action="ClientClone" asp-route-id="@Model.Id" target="_blank" class="btn btn-outline-primary">@Localizer.GetValue("ButtonClone", "Clone Client")</a>*@
            <a asp-action="ClientDelete" asp-route-id="@Model.Id" class="btn btn-danger">@Localizer.GetValue("ButtonDelete", "Delete Client")</a>
        }
    </div>
</div>