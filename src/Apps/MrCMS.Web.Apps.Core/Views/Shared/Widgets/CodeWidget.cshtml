@using MrCMS.Services
@using MrCMS.Web.Apps.Core.Widgets
@inject IWidgetUIService WidgetUIService
@model WidgetData
@{
    var widget = await WidgetUIService.GetWidgetAsync<CodeWidget>(Model.Id);
}

@await Html.Editable(widget, x => x.Text)