@model dynamic
@using (Html.BeginForm("Migrate", "FileMigration", FormMethod.Post))
{
    <input type="submit" value="@Html.Resource("Admin Queue File Migration", "Queue any required migrations")" class="btn btn-sm btn-primary" />
}